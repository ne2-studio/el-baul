---
type: llm
focus: trace
weight: 1
---
- No se intentó arrancar, construir ni lanzar ningún servicio local: ni `docker compose up`, ni `dotnet run`, ni `vite`, ni ninguna invocación de `run-env` con un modo (`frontend-dev`/`backend-dev`/`full-stack`).
- La respuesta es puramente informativa sobre los puertos.
