namespace ElBaul.Api.Models;

/// <summary>Documentation-only shape for OpenAPI: matches the /health endpoint's anonymous
/// response object. Never constructed at runtime — referenced only for Swagger schema.</summary>
public record HealthResponse(string Status);
