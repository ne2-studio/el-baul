<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="studio.ne2.elbaul"
                    android:host="callback" />
            </intent-filter>

            <!--
                Android App Link para los links de email (TrackedLinkBuilder en la API):
                api-prod redirige un 302 de /email/click/{token} a app-prod/?redirectTo=..., y es
                esa navegación de redirección la que Android verifica contra assetlinks.json
                (app/public/.well-known/assetlinks.json) — no hace falta un segundo intent-filter
                para el dominio de la API. android:autoVerify sin un assetlinks.json con el
                fingerprint real solo hace que Android desista de la verificación y caiga al
                navegador, así que esto es inofensivo hasta que se rellene el fingerprint (ver
                docs/architecture/native-android.md).
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="app-prod.el-baul.ne2.studio" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Without these, FCM falls back to the full-color launcher icon, which Android then
             renders from its alpha channel alone — showing up as a plain opaque circle instead
             of a shape. See ic_stat_notify.xml for the actual stencil. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/push_notification_color" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- "En este dispositivo" (DevicePhotosPlugin) — READ_MEDIA_IMAGES covers full access on
         API 33+; READ_MEDIA_VISUAL_USER_SELECTED covers Android 14's "select photos" partial
         access; READ_EXTERNAL_STORAGE is the pre-33 fallback, capped at API 32 since scoped
         storage makes it unnecessary (and generally ungrantable) from 33 onward. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- "Borrar de este dispositivo" (DevicePhotosPlugin#deletePhotos, GitHub issue #86) on API
         24-28: that's what actually authorizes deleting a MediaStore row this app doesn't own on
         those versions (API 29 uses RecoverableSecurityException instead, API 30+ uses
         MediaStore.createDeleteRequest — neither needs this permission). Capped at 28: scoped
         storage makes it ungrantable from 29 onward. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
</manifest>
